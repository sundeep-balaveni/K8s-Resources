apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: roboshop
type: ClusterIP

spec:
  type: ClusterIP
  selector:
    project: roboshop
    app: 

  ports:
  - protocol: TCP
    port: 80  #service port
    targetPort: 80 #container port